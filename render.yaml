services:
  - type: web
    name: netflix-laravel
    env: php
    buildCommand: "composer install --no-dev --optimize-autoloader && php artisan key:generate"
    startCommand: "php artisan serve --host=0.0.0.0 --port=$PORT"
